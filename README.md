# 2D Scene Renderer

Задание не было завершено до конца:
- не добавлен второй объект;
- не знаю успею ли добавить круг;
- не проведено достаточного тестирования, не успел написать unit-тесты (ожидаю проблем на сложных случаях);
- не вполне доволен архитектурой классов.
Выполнено:
- базовый класс Object;
- дочерние классы:Point, Rect, HLine, VLine;
- парсер;
- простой пример;

## Особенности

- Поддерживаемые значения из вводного файла:
  - `point x y`
  - `rect x1 y1 x2 y2`
  - `hline x1 x2 y`
  - `vline y1 y2 x`
  - `circle x y r` (нет)
- Автоматическое масштабирование с сохранением пропорций (максимальная сторона — 1024 пикселя)
- Отрисовка в чёрно-белом цвете (можно задать другие цвета)


## Структура проекта
```bash
├── CMakeLists.txt
├── example
│   ├── files
│   │   └── 1.txt
│   └── main.cpp            # Код примера
├── README.md
├── src
│   ├── CMakeLists.txt      # Собирает библиотеку для задания
│   ├── parser              # Парсер 
│   │   ├── parser.cpp
│   │   └── parser.h
│   ├── scene
│   │   ├── core            # сервисные структуры
│   │   │   ├── color3.h
│   │   │   ├── pixel.h
│   │   │   └── vec2.h
│   │   ├── objects         # объекты
│   │   │   ├── include
│   │   │   │   ├── circle.h
│   │   │   │   ├── hline.h
│   │   │   │   ├── object.h
│   │   │   │   ├── point.h
│   │   │   │   ├── rect.h
│   │   │   │   └── vline.h
│   │   │   └── src_files
│   │   │       ├── circle.cpp
│   │   │       ├── hline.cpp
│   │   │       ├── point.cpp
│   │   │       ├── rect.cpp
│   │   │       └── vline.cpp
│   │   ├── scene.cpp
│   │   └── scene.h         # header сцены
│   ├── simple_bmp.hpp
│   └── third_party         # внешняя библиотека для сохранения bmp
│       └── libbitmap
│           ├── CMakeLists.txt
│           ├── libbitmap.cpp
│           ├── libbitmap.h
│           └── etc...
├── unit_test               # отсутствуют
└── Тестовое задание №1547.docx
```

## Сборка и запуск
### Рекомендуемая последовательность действий:

1. Создайте директорию для сборки проекта:
```bash
mkdir build && cd build
```
2. Из нее сконфигурировать проект:
```bash
cmake ../CMakeLists.txt
```
3. Собрать пример (example):
```bash
make example
```
4. Запустить
```bash
./example 
```
5. В выбранной папке (согласно данной инструкции **build**) появится папка **files** с исходными входными документами и результирующий bmp-файлами.



